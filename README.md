# ai_gpt
To write LLM code from scratch.

## Set up python.
    Install pyenv $> brew install pyenv
    Install python version $> pyenv install 3.14.3
    Set inside project $> pyenv install 3.14.3

## Running projct 
    $> python3 -m <package name>
     ../src $>  python -m app
     ../src $> python -m llm_train

## Running Docker file


## Insstalling pytorch 

    pip install torch==2.10.0 torchvision
